services:
  app:
    develop:
      watch:
        - action: sync+restart
          path: .
          target: /app
          ignore:
            - node_modules/
  
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080